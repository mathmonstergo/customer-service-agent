# 用户确认记录

创建时间：2026-05-06 16:16:01 CST

## 已确认

- 用户确认知识库新功能方向做成“文件导入审核中心”。
- 用户确认风险 1、2、3、4 可以先修复。
- 用户要求后续环境管理使用 Conda。
- 用户确认第一期只做 Markdown，优先适配微信聊天记录；PDF、Word、xlsx 放后续。
- 用户确认上传入口应为通用“上传文件”，系统自动识别格式；第一期只解析 Markdown，其它格式显示暂不支持。
- 用户确认导入审核采用方案 B，但候选 FAQ 详情不常驻页面：页面为文件列表 + 时间切块/候选列表主工作区，候选审核需要点击候选或审核按钮后从右侧抽屉滑出。
- 用户确认视觉效果可以复刻已提供的导入审核页面设计图。
- 用户确认候选 FAQ 包含内部备注字段。
- 用户要求完成后发送邮件通知。

## 待确认

- 实现完成后的真实 Markdown 导入效果。

## 后续确认记录

- 2026-05-07：用户修正候选 FAQ 审核交互，要求与现有 FAQ 编辑界面一致，不能直接常驻显示在页面右侧。
- 2026-05-07：用户体验后反馈标准问答 AI 优化面板、无改动保存触发 embedding stale、导入解析模式、切块勾选和全选问题；确认导入解析需要支持按 1-7 天时间范围切块，默认 1 天。
