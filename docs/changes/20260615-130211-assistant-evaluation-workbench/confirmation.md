# Confirmation

## 2026-06-15 13:02 Asia/Shanghai

用户确认：

* 可以继续下一阶段。
* 下一阶段建议方向为平台问答效果验收工具。

## 后续待确认

当前无阻塞待确认问题。批量运行全部 active 用例已收敛为后续能力，第一版先做单条运行。

## 2026-06-15 13:10 Asia/Shanghai

用户确认：

* 第一版可以做 Approach A：检索评测工作台。
* 用户指出问答页已经展示了部分来源、score、召回通道信息。
* 用户确认评测工作台可以做独立页面。
* 用户明确问答机器人长期方向应该是 agentic。

记录结论：

* 问答页保留“单次问答调试”定位。
* 评测工作台定位为“固定问题集验收”，复用同类展示字段，但新增期望命中判断、Recall@K、MRR、Top1 命中等评测口径。
* 本任务不实现 agentic planner / tool loop，但评测页面和命名应为未来 agentic 运行轨迹评测预留扩展空间。

## 2026-06-15 13:31 Asia/Shanghai

调研后收敛：

* 外部成熟项目普遍将 agentic RAG 定义为“LLM 决策循环 + 工具调用 + 检索/改写/打分/重试/轨迹”，而不是单纯替换向量检索算法。
* 本任务仍只做检索评测工作台，不实现 agentic planner / tool loop。
* 第一版先只做单条评测运行；批量运行全部 active 用例后置。
* 页面和数据命名保留 `strategy` / `run_type` 扩展点，便于未来接入 `agentic_rag_v1`。

## 2026-06-15 UI Layout Confirmation

用户提供评测工作台布局图并确认：

* 可以参考生成图的整体布局和信息层级。
* 右侧“用例编辑”不能作为主页面固定栏，必须做成抽屉。
* 主页面保留评测用例列表、运行结果、候选来源和别名词典/辅助信息。

## 2026-06-15 17:00 Asia/Shanghai

用户补充 UI 约束：

* 评测页左栏和整体页面上方布局，包括宽度、高度和元素位置，需要参考智能问答页面。
* 后续需要做流畅页面切换，因此 `/evaluation` 与 `/assistant` 的整体元素位置不宜有明显差异。

记录结论：

* 评测页采用与智能问答页一致的路由内骨架：左侧 240px 工作栏，右侧主面板 header 从相同位置开始。
* 评测用例搜索和状态筛选放在左侧工作栏内，右侧 header 只放当前用例上下文、别名词典和单条运行操作。
* 别名词典继续使用右侧抽屉，不作为常驻右栏挤压主工作区。
