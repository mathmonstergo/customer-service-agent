# Confirmation

## 当前状态

用户已确认选择 FastAPI/ASGI 全量迁移方向，并确认现有 `admin` 命令直接切换到 ASGI；用户也要求项目对外名称改为 `Cyclops`。实现已完成并通过验证，当前等待用户决定是否提交和归档。

## 待确认内容

* 无。

## 确认记录

* 2026-07-06：用户回复“做3”，确认选择 FastAPI/ASGI 迁移方向。
* 2026-07-06：用户回复“全量迁移”，确认本次迁移范围覆盖现有管理后台 API surface。
* 2026-07-06：用户回复“选2”，确认让现有 `cyclops admin` 直接切换到 ASGI，不新增并行启动命令作为默认路径。
* 2026-07-06：用户回复“是的可以开始做吧，项目名称换成Cyclops，不要customer service agent了”，确认开始实现，并要求用户可见项目名改为 `Cyclops`。
* 2026-07-06：用户澄清项目更名需要实际目录级迁移；已将真实 Python 包目录、源码 import、测试和构建输出迁移为 `cyclops/`，不再只保留转发入口。
* 2026-07-06：ASGI 全量迁移、问答性能保护和 Cyclops 品牌更新已完成；后端测试、前端测试、lint、构建、配置检查和 diff 检查均通过。
