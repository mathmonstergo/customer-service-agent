# 确认记录

## 用户确认
- 验收报告呈现后，用户对"从 P0 三项入手"答复：**"可以"**。
- 同时明确要求：**"test少写，等我确认前端后再写，不然白写了"** —— 本次不补单测，等用户前端验收通过后再决定补测。

## 据此决定
- 仅实施 P0 三项（切片标题竖排 / DialogTitle a11y / 页面标题品牌化），P1/P2 不动。
- 不写测试，改动定型（用户验收）后再议。
- 沿用既有基线决定：`dist` 构建产物随源码一起提交（见 `../20260601-092106-web-react-admin/confirmation.md`）。
- 不主动 git commit；如需提交由用户明确指示。

## 安全 / 数据
- 改动均为前端展示与 a11y，未触及 `.env`、system_prompt、上传原件、客户数据、微信 token。
- 竖排修复只作用于展示层，不改写已存储的 `source_text` 与向量，不改变事实内容。
