# 确认记录

## 2026-06-18

用户确认进入下一阶段开发，方向为：让导入文件/解析任务可以选择 chunker，使上一阶段对齐 RAGFlow 的 MinerU 后解析能力可实际使用。

确认口径：

* 项目主打轻量本地部署，但“轻量”指部署/API 接入形态，不是简化切块策略。
* 文档解析层当前默认只接 MinerU。
* 实现要参考 RAGFlow，不重复造简化路由。
* 当前阶段不做自动轻量分流，重点是准确高效和显式可控。
