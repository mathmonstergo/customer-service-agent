# 用户确认记录

## 2026-05-14 12:46:12

- 用户要求：把 RAGFlow、LightRAG 等项目调研按照 AGENTS.md 记录下来。
- 用户要求：开始做第一阶段检索召回能力建设。
- 用户补充判断：第一阶段应该需要意图识别层。

## 待确认

- 是否先做后端检索评测和智能问答链路改造，暂不做完整 UI。
- 意图识别第一版是否采用“规则优先 + Chat 模型兜底”的方式。
- 检索评测集第一批数据来源：用户提供真实问题，或从现有 FAQ 自动生成种子用例。

## 2026-05-14 后续确认

- 用户确认可以按建议方案开始第一阶段。
- 用户强调：需要把前序调研的完整结论写入变更目录，避免后续继续开发时重复调查。
- 已据此新增 `research-notes.md`，记录 RAGFlow、LightRAG、父子 chunk、Contextual Retrieval、GraphRAG 对本项目的完整取舍结论。
- 已按确认口径先实现后端能力和最小接口，暂未做完整管理后台 UI。

## 2026-05-14 会话界面补充确认

- 用户确认按最终设计图实现智能问答会话界面。
- 右侧“流程调试”必须是覆盖式抽屉，不挤压左侧会话历史和中间聊天窗口。
- “LLM 生成中”不实现百分比进度条，因为当前流式生成接口无法可靠获得真实生成进度。
- 新建对话时需要居中弹窗，背景使用已有玻璃雾化遮罩效果。
- 每个会话可独立设置系统提示词；不填写时只使用本地 `system_prompt.txt` 配置。
- 每个会话可设置会话名称；不设置时沿用首个问题生成默认标题。
- 左侧会话历史卡片 hover 时浮出铅笔按钮，点击后打开同一个会话设置弹窗。
- 用户补充确认：会话级系统提示词自定义功能上线后，应删除智能问答链路里代码层面写死的默认系统提示词；没有会话提示词和本地 `system_prompt.txt` 时，不应注入默认 system prompt。
