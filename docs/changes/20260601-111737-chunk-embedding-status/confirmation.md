# 确认记录

## 用户确认
- 验收中用户报告 #15「重新向量」后仍显示「未索引」、两颗圆点不同步，先问「为什么」。
- 我用代码 + 线上库定位根因（`import_chunks` 无 `embedding_status` 列，切片读到的永远是 undefined）后，给出三选项。
- 用户通过选择题答复：**「显示真实索引状态（推荐）」** —— 即改 `list_import_chunks` 联表派生真实状态。

## 据此决定
- 仅改后端取数 SQL（`list_import_chunks`），不改 schema、不改前端、不重建 dist。
- 不写测试，等用户浏览器验收后再议（沿用 [[feedback-lightweight-verification]]）。
- 不主动 git commit；如需提交由用户明确指示。

## 安全 / 数据
- 纯只读联表查询，未触及 `.env`、system_prompt、上传原件、客户数据、微信 token。

## 追加确认（圆点三态 + 文件层）
- 用户要求把圆点重定义为三态：**禁用（文件层或切片层任一）→ 灰并覆盖一切；已嵌入且无新改动 → 绿；其余 → 黄**。已实现于切片层（横滚轴 + 切片工具栏）。
- 随后追问「文件层圆点要不要也改成三态」，用户选择题答复：**「也改成三态」**（接受语义由「解析状态」变为「嵌入状态」、丢解析进度信号）。据此改了文档列表行 + 抽屉头部，并为解析轮询接口附加 `embedding_summary`（纯附加字段，不改 schema）。
- 仍不写测试，等用户浏览器验收后再议（[[feedback-lightweight-verification]]）；不主动 git commit。
- 临时禁用某文件以验证「文件层灰覆盖」，验毕已还原 `is_disabled=False`，未改动任何真实业务数据。
