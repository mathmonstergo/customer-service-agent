# 用户确认记录

## 2026-05-13 15:53

用户要求继续按照项目 AGENTS.md 开发，并明确提出：

- 在 FAQ 管理页上方 `FAQ 列表` 位置新增 `FAQ 自动生成` 和 `FAQ 审核`。
- 顶部为按钮样式，选中项采用简约的“中间粗、两头细”下划线。
- FAQ 自动生成页面删除导入/上传文件按钮，替换成“选择文件”，且只能选择已经解析的文件。
- 切片详情展开后，切片文本框内只保留切片原文；删除序号、`xx 段内容`、`正文`、`可用于 FAQ 生成` 等正文外信息。
- 查看切片内容时，切片查看框需要有独立上下滚动，不应使用整个弹窗滚动。

## 后续变更点

- 若实现中发现现有导入审核工作区不能直接嵌入 FAQ 管理页，将优先做前端结构迁移，不改变后端数据模型。
- 若 `completed` 状态文件仍有可用切片，也视为可选择的已解析文件。

## 2026-05-13 16:15

已按上述确认内容完成实现，未新增数据库 schema，未改变后端候选生成与保存逻辑。FAQ 自动生成和 FAQ 审核复用现有导入审核数据流，但入口和文案已经收敛到 FAQ 管理页。

## 2026-05-13 16:52

用户继续要求修正 FAQ 列表、FAQ 自动生成、FAQ 审核三个按钮区域的高度差，并移除选中态中间矩形，只保留下划线。同时希望切换时有左右翻页的丝滑动效。已按此方向完成样式和交互调整。

## 2026-05-13 17:02

用户澄清“高度”指右栏整体页面起始位置，不是按钮高度。已确认根因是自动生成/审核右栏存在旧的 `margin-top: -44px`，导致右栏上移到面包屑区域。已移除负偏移和额外高度补偿，并用浏览器测量确认 FAQ 列表与自动生成右栏 top 一致。

## 2026-05-13 17:22

用户继续要求重新审视 FAQ 管理三个子页面并统一风格。已按 FAQ 列表页作为基准收敛：左栏内边距一致，右栏面板贴齐 324px 分栏边界，自动生成/审核的概览、进度和工作区面板不再保留额外左侧外边距；同时将左右滑入动效限制在右侧主内容区，避免外层 grid 平移产生临时横向滚动条和宽度闪烁。
