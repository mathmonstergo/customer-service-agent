# Confirmation

## 2026-06-18

* 用户确认希望评测候选支持查看 FAQ、查看文档切片、复制 ID。
* 用户希望复用已有 FAQ/文档抽屉样式，并考虑统一全平台抽屉宽度。
* 用户确认复制 ID 采用 ID 右侧 icon 按钮，不采用点击 ID 本身复制。
* 用户要求先检查 FAQ 和切片抽屉；特别是切片顶部按钮，必要时优化成单 icon + hover 文案。
* 已检查现有按钮布局，文档级和切片级按钮在收窄后需要 icon 化和重新排布。
* 用户指出文档抽屉 Chunker 原生下拉框暗色主题不可读，需要一并修复。
* 用户确认平台按钮、抽屉、弹窗、提示等 UI 元素要统一：优先复用已有控件，icon-only 或短文字+icon，避免每个新页面创造一套结构。
* 本次实现采用：FAQ/评测抽屉 `520px`，文档切片抽屉 `560px` 过渡；文档抽屉按钮改为 icon/短文案组合。
* 本次实现采用：候选行主要展示可读来源，内部 ID 只作为排查信息并通过右侧复制 icon 复制。
* 本次实现采用：Chunker 选择器改为现有 Popover/Button 控件，避免暗色主题下原生下拉不可读。
