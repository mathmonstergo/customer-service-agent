# 用户确认记录

## 2026-05-08 11:12:45

用户提供三张最终效果图并确认：

- 可以按图实现导入审核工作台。
- 解析页面里的“状态范围”筛选用户认为没有意义，本次不实现。
- 其它布局和功能方向可进入实现。

## 2026-05-08 11:15

用户补充确认：

- “保存到标准问答”按钮需要和 embedding 生成耦合。
- 候选 FAQ 保存后不再要求用户单独点击生成 embedding。

## 2026-05-08 11:20

用户补充确认：

- 第三张候选 FAQ 审核图右下方的“重要程度”实际应理解为“重复度”，UI 图画错了。
- 当前标准问答的新建/编辑界面也应该和新的审核抽屉效果同步。

## 2026-05-08 13:12

用户补充确认：

- 先一张一张收敛，不继续扩散到其它页面。
- 当前优先复刻第一张“导入审核 / 解析块”主页面，要求右侧主工作区和截图尽量一比一对齐。

## 2026-05-08 15:36

用户补充确认：

- 继续复刻“候选 FAQ”页。
- 候选 FAQ 是人工审核，不需要“预计耗时 / 预计剩余”信息。
- 解析块和候选 FAQ 顶部不再重复展示“已解析 xx/xx / 已审核 xx/xx”，状态计数卡即可说明进度。
- 从某个解析块点击进入候选 FAQ 时，要自动带入来源块编号，只筛该块自己的候选 FAQ。
- 解析块和候选 FAQ 的外部框架要保持高度一致，只做内部指标、进度条和表格内容的动态变化；后续切换需要支持类似 iOS 的滑动进入和进度条归零再增长动效。

## 2026-05-08 17:42

用户补充确认：

- 解析块和候选 FAQ 页面未打开详情编辑页时，不能通过横向滑动看到隐藏在右侧的抽屉。
- 详情编辑页应只在打开时从右侧滑出；关闭态要从页面交互和可视区域中消失。
- 除关闭按钮外，点击详情页外部任意区域也应让抽屉滑回并关闭。
